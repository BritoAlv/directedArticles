## Projection in a Subspace

Projection in a single direction, can be generalized: the general question, given a set of vectors $v_1, v_2, ..., v_d$ as columns of a matrix $M$, what's the matrix that indicates the linear transformation of a vector $v$ to its projection on the space spanned by $M$?

That matrix is called the pseudo-inverse of $M$.

